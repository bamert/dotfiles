#!/usr/bin/env bash
set -eu
curl -LO https://github.com/neovim/neovim/releases/latest/download/nvim-linux64.tar.gz
rm -rf $LOCAL_INSTALL_PREFIX/nvim-dl
cd $LOCAL_INSTALL_PREFIX
tar -xzf nvim-linux64.tar.gz
